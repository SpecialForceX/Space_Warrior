class EnergieBeamProtection extends MovableObject {

    constructor(x, y) {
        super().loadImg('img/enemys/boss/energy_beam/protection.png');
        this.x = x;
        this.y = y;
        this.width = 215; // Beispielbreite
        this.height = 260; // Beispielhöhe
    }

}
